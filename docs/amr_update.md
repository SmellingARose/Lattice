We want to fix AMR to be inside out instead of outside in.

We can have a different C (refinement radius scale factor) per level, and therefore a different refinement radius for each level. This allows independent control of wave zone coverage vs puncture resolution, instead of a single C that forces both from one formula.
